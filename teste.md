| Policy | Description | Security Interest? | Security Purpose | Existing Requirement | Plugin Candidate? | Notes |
|--------|-------------|--------------------|------------------|----------------------|-------------------|-------|
| GenerateJWT | Generates a signed JSON Web Token (JWT). | Yes | Issues signed JWTs for authentication or identity propagation. | Access token type (backend), API / Backend authentication | No | Security mechanism used to generate JWTs, but its use depends on the API security architecture. |
| VerifyJWT | Verifies the signature, validity, and claims of a JSON Web Token (JWT). | Yes | Authenticates and validates JWT access tokens before processing requests. | Access token control | Yes (Already implemented) | Already covered by the existing Access Token Control (EX-CS009) plugin. |
| DecodeJWT | Decodes a JSON Web Token (JWT) without verifying its signature. | Yes (Indirect) | Extracts JWT header and claims for later processing. | Access token control (supporting only) | No | Does not provide any security guarantee by itself; should normally be used together with VerifyJWT. |