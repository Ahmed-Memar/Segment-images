| Policy | Description | Security Interest? | Security Purpose | Existing Requirement | Plugin Candidate? | Notes |
|--------|-------------|--------------------|------------------|----------------------|-------------------|-------|
| MonetizationLimitsCheck | Verifies that API consumers have not exceeded the limits defined by their monetization plan. | No | — | — | No | Business and monetization policy; not a security control. |
| MessageLogging | Sends request, response, or flow information to an external logging system. | Yes (Indirect) | Supports auditability, monitoring, and incident investigation. | End-to-end API call tracking | No | Logging is important for security operations, but the policy alone does not prove traceability requirements are satisfied. |
| LDAP | Performs LDAP operations against an external directory service. | Yes | Can authenticate users or retrieve identity and authorization information from an LDAP directory. | Client authentication, Coarse-grained authorization | No | The security purpose depends on how the LDAP query is used within the API flow. |
| KeyValueMapOperations | Reads from or writes to an Apigee Key Value Map (KVM). | Yes (Indirect) | Can securely retrieve configuration values, API keys, or secrets used by security mechanisms. | Multiple (context-dependent) | No | KVMs may store sensitive or non-sensitive data; the policy itself does not enforce security. |