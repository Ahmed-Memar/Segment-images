Some TLS indicators are visible in the bundle, but the complete backend TLS configuration is managed outside the proxy bundle.


The bundle cannot reliably prove whether TLS is enabled and correctly configured toward the backend because TargetServer configuration is external to the bundle.