| Matrix column name | Verifiable from API proxy only? | Why | Relevant Policies | How? | Automate? | Why? |
|-------------------|----------------------------------|-----|-------------------|------|-----------|------|
| Client authentication | Partial | Authentication mechanisms can be detected in the proxy, but whether client authentication is required depends on API context, sensitivity, and trust level, which are not available in the bundle. | OAuthV2 (VerifyAccessToken), VerifyJWT, VerifyAPIKey, BasicAuthentication, SSLInfo (mTLS indicators) | Not implemented. Mostly covered by Access Token Control for OAuth2/JWT-based authentication. | No | A generic plugin can detect authentication mechanisms, but cannot determine whether the selected mechanism is mandatory or appropriate for the API context. |
| OAuth2 grant | Partial | OAuth2 grant type is visible only when the proxy generates tokens. If tokens are issued by an external Authorization Server, the grant type is not visible in the bundle. | OAuthV2 (GenerateAccessToken) only when the proxy acts as Authorization Server | Not implemented. | No | In most cases, the grant type is managed outside the API proxy and cannot be reliably determined from the bundle alone. |
| OpenID Connect flow | Partial | OIDC authentication usually occurs between the client and the Identity Provider. The complete flow is not visible in the API proxy bundle. | VerifyJWT (ID token validation), ServiceCallout (external introspection if used) | Not implemented. | No | The proxy may validate an ID token, but cannot reliably prove that a compliant OIDC flow is used end-to-end. |
| Access token type | Partial | The token type (JWT or opaque) can often be inferred from token validation policies, but the expected type depends on API context and security requirements. | VerifyJWT, OAuthV2 (opaque token introspection/validation) | Not implemented. | No | The bundle may reveal the token type in use, but cannot determine whether it matches the expected security requirement. |
| Refresh token lifespan | Partial | Refresh token lifetime is usually defined by the Authorization Server and is not visible in the API proxy bundle. If token issuance is implemented in the proxy, the lifetime may be configured there. | OAuthV2 (GenerateAccessToken / RefreshTokenExpiresIn) only when the proxy acts as Authorization Server | Not implemented. | No | In most cases, the value is managed outside the proxy and cannot be reliably verified from the bundle. |
| Access token lifespan | Partial | Access token lifetime is usually defined by the Authorization Server and is not visible in the API proxy bundle. If token issuance is implemented in the proxy, the lifetime may be configured there. | OAuthV2 (GenerateAccessToken / ExpiresIn) only when the proxy acts as Authorization Server | Not implemented. | No | In most cases, the value is managed outside the proxy and cannot be reliably verified from the bundle. |