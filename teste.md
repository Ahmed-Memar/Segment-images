| Policy | Description | Security Interest? | Security Purpose | Existing Requirement | Plugin Candidate? | Notes |
|--------|-------------|--------------------|------------------|----------------------|-------------------|-------|
| HMAC | Computes or verifies an HMAC signature to ensure message authenticity and integrity. | Yes | Protects message integrity and authenticates the sender using a shared secret. | Data integrity control | No | Strong security mechanism, but its presence alone does not prove that HMAC is required or correctly enforced for the API. |
| JavaCallout | Executes custom Java code during API processing. | Yes (Indirect) | Can implement custom security controls such as authentication, authorization, encryption, or validation. | Multiple (context-dependent) | No | The security purpose depends entirely on the custom Java implementation, which cannot be generically analyzed. |
| JavaScript | Executes custom JavaScript code during API processing. | Yes (Indirect) | Can implement custom security logic such as authentication, authorization, validation, or logging. | Multiple (context-dependent) | No | The security purpose depends entirely on the script logic and cannot be reliably inferred from the policy alone. |