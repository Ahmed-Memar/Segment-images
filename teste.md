| Matrix column name | Verifiable from API proxy only? | Why | Relevant Policies | How? | Automate? | Why? |
|-------------------|----------------------------------|-----|-------------------|-------|-----------|------|
| DDoS prevention (Internet-facing APIs only) | Partial | Rate limiting via SpikeArrest is visible in the proxy bundle, but infrastructure-level DDoS protection (WAF, CDN, network protections) is not. | SpikeArrest | Verify that a SpikeArrest policy exists and is enforced in the PreFlow or across all applicable request flows. | Yes | A proxy bundle can reliably verify the presence and enforcement of SpikeArrest, which provides API-level rate limiting. |
| Access token control | Yes | Token validation is implemented through explicit validation policies (OAuthV2 VerifyAccessToken or VerifyJWT) that can be statically analyzed from the proxy bundle. | OAuthV2 (VerifyAccessToken), VerifyJWT | Verify that at least one access token validation mechanism exists and is enforced in the PreFlow or all applicable request flows. Validate VerifyJWT configuration when used. | Yes | The required controls are represented by explicit Apigee policies and can be reliably verified through static analysis. |
| Coarse-grained authorization enforcement | Partial | Authorization may be implemented through scopes, claims, flow conditions, or custom logic, and the required authorization model depends on API context and sensitivity. | OAuthV2 + GetOAuthV2Info, VerifyJWT, Flow Conditions, ExtractVariables, RaiseFault | Not implemented. | No | A generic plugin cannot reliably determine whether authorization rules are present, complete, or appropriate for the business context. High false-positive and false-negative risk. |
| End-to-end API call tracking | Partial | The proxy can generate or propagate correlation identifiers, but end-to-end traceability across downstream systems, logging platforms, and backends cannot be verified from the proxy bundle alone. | AssignMessage, ExtractVariables, MessageLogging, JavaScript | Not implemented. | No | Presence of tracking headers or logging policies does not guarantee end-to-end traceability. Verification requires runtime and backend visibility. |