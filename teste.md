| Matrix column name | Verifiable from API proxy only? | Why | Relevant Policies | How? | Automate? | Why? |
|-------------------|----------------------------------|-----|-------------------|-------|-----------|------|
| Data integrity control | Partial | TLS-related indicators may be visible in the proxy bundle, but message integrity mechanisms (signatures, HMAC, certificates, backend validation) are API-specific and cannot be reliably verified from the proxy bundle alone. | ProxyEndpoint VirtualHost, TargetEndpoint SSLInfo | Not implemented. | No | The bundle cannot reliably prove that data integrity is enforced end-to-end. Integrity controls may be implemented outside the proxy or through custom application logic. |
| Exception & error management | Yes | Error handling mechanisms are explicitly defined through FaultRules and DefaultFaultRule in the proxy configuration. | FaultRules, DefaultFaultRule | Verify that a non-empty DefaultFaultRule exists. If only non-empty FaultRules are present, report a warning. If no valid error-handling mechanism exists, report an error. | Yes | The control relies on explicit Apigee constructs that can be reliably verified through static analysis of the proxy bundle. |