| Matrix column name | Verifiable from API proxy only? | Why | Relevant Policies | How? | Automate? | Why? |
|--------------------|--------------------------------|------|-------------------|-------|-----------|------|
| Client authentication | Partial | Authentication mechanisms can be detected in the proxy, but whether client authentication is required depends on API context, sensitivity, and trust level, which are not available in the bundle. | OAuthV2 (VerifyAccessToken), VerifyJWT, VerifyAPIKey, BasicAuthentication, SSLInfo (mTLS indicators) | Not implemented. Mostly covered by Access Token Control for OAuth2/JWT-based authentication. | No | A generic plugin can detect authentication mechanisms, but cannot determine whether the selected mechanism is appropriate or mandatory for the API context. |
| OAuth2 grant | Partial | OAuth2 grant type is only visible when the proxy itself generates tokens. If tokens are obtained from an external Authorization Server, the grant type is not visible in the proxy bundle. | OAuthV2 (GenerateAccessToken) only when the proxy acts as Authorization Server | Not implemented. | No | In most cases the grant type is managed outside the API proxy and cannot be reliably determined from the bundle alone. |
| OpenID Connect flow | Partial | OIDC authentication flow usually occurs between the client and the Identity Provider. The complete flow is not visible in the API proxy bundle. | VerifyJWT (ID Token validation), ServiceCallout (external introspection) | Not implemented. | No | The proxy may validate an ID token, but cannot reliably prove that a compliant OIDC flow was used end-to-end. |