### Request Flows

POST, PUT and PATCH request flows are assumed to process JSON request bodies by default, unless explicit XML processing is detected.

A JSONThreatProtection policy must be applied in the same flow; otherwise, an error is reported.