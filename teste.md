| Policy | Description | Security Interest? | Security Purpose | Existing Requirement | Plugin Candidate? | Notes |
|--------|-------------|--------------------|------------------|----------------------|-------------------|-------|
| BasicAuthentication | Generates an HTTP Basic Authentication header (`Authorization: Basic ...`) using a username and password. | Yes | Authenticates Apigee to the backend using HTTP Basic Authentication. | API / Backend authentication, Credential type (backend) | No | Supports backend authentication, but its absence does not indicate non-compliance because other mechanisms (OAuth2, JWT, mTLS, etc.) may be used instead. |