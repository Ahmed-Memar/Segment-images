| Matrix column name | Verifiable from API proxy only? | Why | Relevant Policies | How? | Automate? | Why? |
|-------------------|----------------------------------|-----|-------------------|-------|-----------|------|
| Injection attacks prevention | Yes | Threat protection policies are explicitly configured in the proxy and can be statically analyzed from the bundle. | JSONThreatProtection, XMLThreatProtection | Verify that the appropriate ThreatProtection policy exists and is enforced on all applicable flows processing JSON or XML payloads. Validate required security parameters. | Yes | The control relies on explicit Apigee policies whose presence, coverage, and configuration can be reliably verified through static analysis. |
| HTTP methods control | Yes | Allowed HTTP methods are enforced through flow conditions and fault handling logic that are visible in the proxy bundle. | Flow Conditions, RaiseFault | Verify that only approved HTTP methods are accepted and that unsupported methods are rejected. | Yes | Method restrictions are implemented through explicit proxy configuration and can be reliably verified from the bundle. |
| HTTP response sanitizing | Partial | The proxy may remove or overwrite response headers, but completeness of sanitization depends on implementation details, backend behavior, and organizational requirements. | AssignMessage, JavaScript | Not implemented. | No | A generic plugin cannot reliably determine whether all sensitive information is sanitized. High false-positive and false-negative risk. |
| Data schema controls | Yes | Schema validation is explicitly implemented through dedicated validation policies attached to proxy flows. | OASValidation, SOAPMessageValidation | Verify that the appropriate schema validation policy exists and is enforced on all applicable request flows. Validate required configuration parameters. | Yes | The control relies on explicit validation policies whose presence, coverage, and configuration can be statically verified. |




The proxy may remove or overwrite response headers, but completeness of sanitization depends on implementation details, backend behavior, and organizational requirements.