| Matrix column name | Verifiable from API proxy only? | Why | Relevant Policies | How? | Automate? | Why? |
|-------------------|----------------------------------|-----|-------------------|------|-----------|------|
| Credential type | Partial | Accepted authentication mechanisms can be detected from proxy policies, but whether the selected credential type is appropriate depends on API sensitivity, trust boundaries, and backend requirements that are not available in the bundle. | OAuthV2, VerifyJWT, VerifyAPIKey, BasicAuthentication | Not implemented. Covered by EX-CS009 Access Token Control for OAuth2/JWT validation mechanisms. | No | Authentication mechanisms can be detected, but a generic plugin cannot determine whether the selected credential type complies with the expected security model. |
| TLS | Partial | HTTPS/TLS indicators may be visible in the proxy configuration (VirtualHost references), but complete TLS configuration is managed outside the proxy bundle. | ProxyEndpoint (VirtualHost - HTTPS) | Not implemented. | No | The bundle cannot reliably prove that TLS is enabled, enforced, or correctly configured on the exposed endpoint. |
| Client / API authentication | Partial | Authentication policies are visible in the proxy, but whether authentication is required depends on API context, sensitivity, and trust level. | OAuthV2, VerifyJWT, VerifyAPIKey, BasicAuthentication | Not implemented. Covered by EX-CS009 Access Token Control for OAuth2/JWT validation mechanisms. | No | A generic plugin cannot determine whether the detected authentication mechanism is appropriate or mandatory for the API context. |
| Consumer app. IP whitelisting | Partial | IP filtering may be implemented in the proxy or externally through infrastructure controls. | AccessControl, Flow Conditions, RaiseFault, JavaScript (custom allowlist logic), ExtractVariables | Not implemented. | No | Absence of IP filtering logic in the proxy does not prove that IP filtering is not enforced elsewhere. |
| DDoS prevention (Internet-facing APIs only) | Partial | API rate-limiting mechanisms such as SpikeArrest are visible in the proxy bundle. However, DDoS protection may also rely on external infrastructure controls. | SpikeArrest | Verify that a SpikeArrest policy exists and is enforced on applicable flows. | Yes | SpikeArrest presence and coverage can be reliably verified from the proxy bundle. |
| Access token control | Yes | Token validation mechanisms are explicitly configured in the proxy. | OAuthV2 (VerifyAccessToken), VerifyJWT | Verify that at least one access token validation mechanism exists and is enforced. Validate VerifyJWT and OAuthV2 configuration parameters. | Yes | Explicit Apigee token validation policies can be reliably verified. |
| Coarse-grained authorization enforcement | Partial | Authorization may be implemented through scopes, claims, flow conditions, or custom logic, and the required authorization model depends on API context and sensitivity. | OAuthV2 + GetOAuthV2Info (scopes), VerifyJWT (claims validation), Flow Conditions, ExtractVariables, RaiseFault | Not implemented. | No | A generic plugin cannot reliably determine whether authorization enforcement is sufficient for the business requirements. |
| End-to-end API call tracking | Partial | Correlation identifiers may be generated, propagated, or logged by the proxy, but end-to-end traceability across downstream systems cannot be verified. | AssignMessage (propagate correlation headers), ExtractVariables, MessageLogging, JavaScript (custom correlation logic) | Not implemented. | No | Correlation IDs may be present, but the bundle cannot prove end-to-end traceability across all downstream systems. |
| Injection attacks prevention | Yes | Threat protection policies are explicitly configured in the proxy. | JSONThreatProtection, XMLThreatProtection | Verify that the appropriate ThreatProtection policy exists, is enforced on all applicable flows, and contains required security parameters. | Yes | Explicit Apigee threat protection policies can be reliably verified. |
| HTTP methods control | Yes | Allowed HTTP methods are enforced through flow conditions and request handling logic visible in the proxy bundle. | Flow Conditions, RaiseFault | Verify that only approved HTTP methods are accepted and that unsupported methods are rejected. | Yes | HTTP method restrictions can be reliably verified from the proxy bundle. |
| HTTP response sanitizing | Partial | Response sanitization may be implemented through different mechanisms and custom logic. | AssignMessage (remove/set headers), JavaScript (custom allowlist/blocklist logic), Flow Conditions | Not implemented. | No | A generic plugin cannot reliably determine whether response sanitization is sufficient or complete. |