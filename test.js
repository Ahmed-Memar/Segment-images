/**
 * Extracts JSONThreatProtection policies from a list of steps.
 *
 * @param {Object} endpoint - Apigee endpoint object
 * @param {Array<Node>} steps - List of Step XML nodes
 *
 * @returns {Array<Object>} - List of JSONThreatProtection policy objects
 */


/**
 * Checks if any step in the list uses JSON processing.
 *
 * @param {Object} endpoint - Apigee endpoint object
 * @param {Array<Node>} steps - List of Step XML nodes
 *
 * @returns {boolean} - True if at least one step uses JSON
 */